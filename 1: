package main

import "fmt"

func main() {
	a := [...]int{1, 2, 1, 3, 4}
	b := hanshu(a[:])
	fmt.Println(b)
}
func hanshu(a []int) map[int]int {
	m := make(map[int]int)
	for i := range a {
		m[a[i]]++
	}
	return m
}
